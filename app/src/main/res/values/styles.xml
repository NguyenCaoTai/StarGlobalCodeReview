<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="UserCard.ButtonAction" parent="Widget.AppCompat.Button.Small">
        <item name="android:layout_width">@dimen/user_button_action_size</item>
        <item name="android:layout_height">@dimen/user_button_action_size</item>
        <item name="android:layout_margin">@dimen/user_button_action_margin</item>
    </style>

</resources>